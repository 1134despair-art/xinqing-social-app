# 内部技术核对记录

记录日期：2026 年 9 月 8 日；整理日期：2026 年 9 月 10 日。

本文件保留上一轮的代码证据及技术核对事项，供开发、运维查阅。以下 P01-P21 是历史索引，不再是需要业务负责人逐项填写的清单；文中的“仍需提供”均按历史核对语境理解。

当前需补充的资料以 [精简资料清单](completion-checklist.md) 为准。对外文件为 [用户协议](user-agreement.html) 和 [隐私政策](privacy-policy.html)。本次只修改文档，下面记录的代码差异不表示已修复。

## 2026 年 9 月 10 日双平台复核（当前结论）

用户已确认上架国内安卓应用商店及苹果 App Store。以下结论覆盖本文件后面的旧写法和历史 P01-P21 提问；用户不再按历史清单逐项填资料。只需 B01-B04 基础资料，后台另外确认 T01 实际服务接收方及 T02 部署地区。两份协议仍是待发布核对稿，不能凭文案认定通过审核。

### 已修订的文档

- 限定为 Android/iOS App（含 App 内承载业务的网页），移出独立 H5 的浏览器识别及音频附件说明。H5 源码事实仍在历史记录中；日后发布独立 H5 时需要另行覆盖。
- 合并短信、AI、外部存储/媒体和审核的重复待填提示；已有 IM、ASR、X5、DCloud 提供方和完整可点击的官方隐私链接保留。
- 补充 DCloud 官方数据类别，区分随机匿名设备标识与 OAID/IDFA/IMEI/Android ID；未把关闭 uniStatistics 等同于框架不处理信息。
- 补充第三方 AI 接收前告知及明确同意、服务商提供不低于政策承诺的保护、用户举报及复核说明。
- 权利申请和个人信息投诉期限改为自收到后 15 个工作日内完成必要核验、处理并反馈。旧文“核验后才起算、只回复进度、复杂即可延期”的表述已停用；依法保留或技术上暂时难以删除的情况说明原因并限制处理。
- 保留实际尚无应用内注销入口、反馈仅存本地、录音发布前上传等事实。文档中的新承诺需实施后方可发布。

### 代码与提审阻碍

| 核对项 | 当前证据 | 发布前处理 |
| --- | --- | --- |
| App 内协议与未登录可读性 | `pages/settings/about.vue:54`、`:62` 仍为旧简版硬编码；`App.vue:41` 的登录跳转仅排除登录页。 | 接入完整新协议；未登录、切到后台再返回时也可阅读，不被强制跳走。 |
| 手机号收集与首次启动 | `pages/login/login.vue:113` 的发送验证码流程未检查 `agreed`；登录在 `:141` 才校验。`androidPrivacy.json` 只声明 template。 | 实测首次启动模板是否完整，并落实收集前告知/所需同意；验证码调用不能仅靠后续登录勾选。检查拒绝时的 SDK 初始化和数据发送。 |
| 注销 | `api/auth.js` 只有验证、登录、换绑和退出等接口；未找到应用内账号注销入口或删除接口。 | 实现应用内发起删除、账号及关联 UGC/媒体的后台删除、结果通知。邮件可作补充渠道，不能作为普通 iOS App 唯一注销方式。 |
| 用户内容安全 | `pages/space/detail.vue` 有心情和评论举报，`api/report.js` 提交 `/api/reports`；未找到屏蔽或拉黑用户的实现。审核状态不能证明后台有过滤和及时处置。 | 验证内容过滤及举报处置，补齐屏蔽骚扰用户、可访问的联系渠道；不把当前心情匹配直接认定为随机匿名聊天。 |
| 第三方 AI 同意 | `pages/publish/publish.vue:575` 开关仅修改 `enableVideo`，`:562` 还会在转换视频场景启用；未见具体接收方和发送信息的同意步骤。已有心情生成见 `:1925`。 | 确认模型接收字段、接收方和规则；在实际发送前明确告知并征得同意，覆盖全部入口；核实 AI 生成合成内容标识。 |
| 权限与必要性 | `manifest.json` 额外声明 READ_PHONE_STATE、GET_ACCOUNTS、READ_LOGS、WRITE_SETTINGS、MOUNT_UNMOUNT_FILESYSTEMS 等；`pages/settings/edit-profile.vue:194` 等要求年龄、性别，允许年龄 1-120。 | 按正式包实际调用清理无关权限；复核相册选择方式、iOS 用途文案和必填资料必要性；不能在协议中把当前必填项写成可选。 |
| 本地表单 | `pages/profile/sub.vue:785`、`:806` 的反馈和替换请求写入本地 serviceRecords，`:623` 持久化，无提交服务端的调用。 | 接通真实受理或修正提示。不能让用户把本地“成功”提示当作投诉已送达。 |
| 未成年人 | 年龄允许 1-120，未发现完整监护人授权及儿童规则落实。 | 按目标年龄范围落实年龄限制或儿童保护流程。一般“监护人指导”条款不等于已取得监护人同意。 |
| 后台可用性 | 《C端接口 (App).md》对短信和部分生成能力仍写预留；没有后台实现、正式 APK/IPA/AAB 可验证。 | 以正式服务验证验证码、生成、举报和删除闭环，准备有效的审核访问方式；不把接口文档等同于线上事实。 |

### 更新后的数据流证据

- 头像：`api/user.js:71` 起先走 `/api/user/avatar` 上传绑定；缺少 URL 时查资料，仍失败再走 `/api/upload/avatar` 加资料更新。覆盖历史只描述通用上传的记录。
- 客服：`api/im.js` 的 `/messages/prepare` 先把消息交给业务后台，腾讯 IM 再传递文字/附件元信息；附件由 `/api/im/upload` 上传。IM 厂商不能证明附件实际托管于腾讯云。
- 录音：`pages/publish/publish.vue:829` 负责上传，`:900` 触发自动上传，`:915` 为录音停止处理。发送数据构建见 `:1698` 起，转文字方式也附已有音频地址和时长。不能承诺只在点击发布后上传。
- iOS：`uni_modules/tencent-speech-asr/utssdk/app-ios/index.uts:8` 返回不支持实时识别，不应披露为与 Android 相同的实时识别流程。
- 网络：`utils/request.js:17` 当前 baseURL 为 HTTPS；前一行 HTTP 地址是注释，不作为当前明文传输证据。实际媒体 URL、SDK 通信和发布包仍需验证。
- 存储：接口示例中 OSS/MinIO 地址、腾讯 VOD 回调和通用视频封面规则不能确认实际供应商、地域或跨境情况。无需用户提供密钥，只需后台确认 T01/T02。

### 验收依据与范围

本次已查阅 [Apple App Review Guidelines](https://developer.apple.com/app-store/review/guidelines/)（页面显示更新于 2026-06-08）1.2、2.1、5.1.1 和 5.1.2(i)，以及 [Apple 账号删除要求](https://developer.apple.com/support/offering-account-deletion-in-your-app/)。普通应用必须提供 App 内发起删除，第三方 AI 数据提供前需明确告知并取得明确同意。

国内参照官方 [《App 违法违规收集使用个人信息行为认定方法》](https://www.cac.gov.cn/2019-12/27/c_1578986455686625.htm)，特别是收集前告知与同意、非必要权限，以及第六部分对注销、权利申请和投诉处理的要求。15 个工作日承诺包含必要核验与处理，不在身份核验完成后才启动计时。

正式发布还需：依据安装包核对 Android 商店 SDK/权限清单与苹果隐私申报及适用的隐私清单文件；按真实年龄范围分级；使用无需登录可访问的 HTTPS 政策链接；落实 App 备案及与实际 AI 业务有关的标识和适用要求。本次没有作全量监管资质、后台安全或安装包合规认证。

本轮只改文档，未修复上述代码，未运行原生 App 或验证线上删除、SDK 流量、控制台和部署区域。协议发布前需用实际修复结果更新注销路径、必填资料和服务范围，移除已解决的待填标记及核对稿提示。

---

## 上一轮采用的写法（历史）

- 对外正文只保留基础主体/联系/日期信息，以及必要的后台接收方与地域补充项。
- 额度采用服务器显示及具体活动规则；注册赠 3、邀请加 1等文档约定仍在下文保留，待后台验证后再作为具体活动规则公示。
- 保存期限采用按处理目的、账号/内容生命周期和适用法律确定的规则，未编造服务器清理天数。
- 通过联系邮箱提出注销、复制、删除等申请，并拟定必要身份核验后原则上 15 个工作日内答复。这是参照样稿起草的运营承诺，需落实实际受理和处理流程。
- 未成年人条款使用一般监护和儿童信息保护要求，未宣称已经实现年龄核验或监护人同意系统。
- IM、ASR、X5 和 DCloud 的官方提供方及专项隐私链接已通过官网核实，见下节；其他后台实际供应商和本项目部署、配置差异仍待确认。

---

## 2026 年 9 月 10 日官方链接核实

- **腾讯云即时通信 IM：**腾讯云计算（北京）有限责任公司；[IM SDK 个人信息保护规则](https://cloud.tencent.com/document/product/269/58094)。适用于当前客服 IM 接入；官网最近更新：2025-06-25。
- **腾讯云语音识别 ASR：**腾讯云计算（北京）有限责任公司；[语音识别 SDK 个人信息保护规则](https://cloud.tencent.com/document/product/1093/73072)。适用于当前 Android 实时识别；官网最近更新：2024-12-23。
- **腾讯 X5 网页内核：**深圳市腾讯计算机系统有限公司；[X5 网页引擎 SDK 个人信息保护规则](https://rule.tencent.com/rule/1c4e2b4b-d0f6-4a75-a5c6-1cfce00a390d)。仅在正式安装包实际启用时适用；官网更新：2025-10-22，生效：2025-10-29。
- **DCloud App 引擎：**数字天堂（北京）网络技术有限公司；[DCloud App 引擎隐私政策](https://dcloud.io/license/appprivacy.html)。适用于 uni-app 等客户端引擎；官网更新：2025-04-14。

官方已披露的地域规则：IM、ASR 和 X5 的上述专用规则均声明将在中华人民共和国境内收集和产生的个人信息存储于境内；DCloud App 引擎政策还声明不将其境内运营收集的信息传输至境外。ASR 服务条款第 2.12、2.13 条明确本服务的数据处理地点为境内/中国大陆。以上为相应服务的官方承诺，仍需核对本项目是否使用对应服务区域；不据此推定业务数据库、上传文件、AI 或其他后台服务也全部在境内。

ASR 另有[语音识别服务条款](https://cloud.tencent.com/document/product/301/94121)，第 2.11 至 2.13 条说明受托处理、问题排查数据用途及境内处理地点。已查明的公开规则无需重复填写；账号是否另行授权数据使用、实际留存和删除配置仍需核实。

[腾讯云隐私保护声明](https://cloud.tencent.com/document/product/301/11470)第 1.3 条排除开发者利用腾讯云向其用户提供服务时涉及的个人信息处理活动，因此不将该通用声明作为本 App 所有腾讯云业务的统一隐私依据。IM、ASR 使用上面的 SDK 专项规则；短信、AI、存储和审核需先确认具体产品。

X5 旧地址 `https://x5.tencent.com/docs/privacy.html` 实际跳转首页；本次从官网“全部协议”的开发者协议进入 SDK 专项规则，已保存跳转后的正式规则地址。DCloud 原链接为社区服务条款，已改用 App 引擎专用政策。

以上是公开规则核实，不等于访问过云控制台或验证过生产部署。IM 的推送插件、X5 的可选定位等不得因官方规则列出就写成本 App 已启用；现有版本是否还有差异由打包核实。下文历史“仍需提供”中涉及这些已查明的主体和链接的内容已被本节更新覆盖。

## 历史逐项核对全文

核对日期：2026 年 9 月 8 日  
代码配置：橘子圆梦 1.0.14 / versionCode 119  
对应文件：[用户协议](user-agreement.html)、[隐私政策](privacy-policy.html)

本次已重新核对客户端源码、打包配置、依赖锁文件、原生插件和《C端接口 (App).md》。代码能查明的内容已填入，不需要重复提供。P01-P21 继续对应协议中的占位编号，但每项只保留实际未知的部分。

证据分为三类：

- **代码已确认**：当前源码存在调用、页面或配置；配置声明不等于已在正式安装包启用。
- **接口文档约定**：项目内接口文档已经写明规则；本次未取得后台实现或验证线上接口。遇到与客户端不一致的内容，单独列出。
- **仍需提供**：当前文件夹无法确定的运营信息、后台部署配置或服务商合同信息。

## 一、现在需要你提供的资料

只需填写以下剩余内容，技术信息可转给对应开发或运维人员。详细依据及对应编号见后文。

| 由谁提供 | 仍需提供的内容 | 对应编号 |
| --- | --- | --- |
| 你或公司负责人 | 运营公司法定全称、联系地址、统一社会信用代码；如品牌主体不同，说明关系 | P02 |
| 运营负责人 | 客服工作时间、投诉和申诉处理期限、备用联系方式；筑梦师服务范围、所属公司、是否外包及资质；隐私联系人、邮箱和地址 | P03、P04 |
| 产品、发行 | 实际发布平台和服务地区、App 备案号、正式包标识；协议版本及更新、生效日期；最低使用年龄及未成年人规则 | P01、P05、P06 |
| 产品、后台 | 每日额度具体数量和重置时区、有效邀请认定、失败返还及申诉规则；如另有实际收费业务，补充收费规则 | P07 |
| 运营、后台 | 注销、复制导出、撤回同意及人工删除申请的实际受理渠道、核验步骤、处理期限和结果通知方式 | P08 |
| 后台、运维 | 数据、日志、上传文件、聊天和备份的保存/清理期限，实际存储地域及跨境访问情况 | P09、P10 |
| 接入、采购、打包人员 | 短信是否正式启用及供应商；各实际服务商主体、政策链接、额外采集字段及留存规则；AI 模型、训练用途；正式安装包 SDK 差异 | P11-P18 |
| 产品、后台、安全 | 已列出的权限、同意机制、匹配/审核冲突和访问控制问题的实际处理结果；H5 是否发布及允许的浏览器识别服务 | P18-P21 |

填写格式：`编号：补充内容；确认人/部门：；依据或生效版本：`。不适用的项目可直接写“不适用”及原因。已有代码事实无需重新抄写。

## 二、逐项核对结果

### P01 产品名称、版本与平台

**代码已确认**

- App 配置名称为“橘子圆梦”，版本为 **1.0.14**，版本号为 **119**。
- DCloud App ID：`__UNI__FB205FD`。
- Android 自定义调试基座配置包名：`com.moods.app`。这是开发启动配置，尚不能作为正式上架包名。
- 已有 Android、iOS 和 H5 的代码或构建配置；微信小程序 App ID 为空，不能据配置模板认定小程序已发布。
- H5 标题和部分关于页仍用“心情星球”；“我”页写死的 v1.0.0 与 manifest 版本不同，应统一展示。

**仍需提供**：实际发布平台、服务国家/地区、备案号，以及正式 Android 包名、iOS Bundle ID 和商店主体与上述配置的差异。当前两份协议采用“橘子圆梦”，无需重新填写已有名称和配置版本。

依据：`manifest.json:2`、`.hbuilderx/launch.json:16`、`pages/profile/profile.vue:60`、`pages/settings/about.vue`。App 版本不能替代协议版本。

### P02 运营公司主体

**核对结果**：当前业务源码和配置中未找到可确认的运营公司法定名称、联系地址或统一社会信用代码。DCloud App ID、接口域名及 SDK 厂商名称均不能确定应用运营主体。

**仍需提供**：公司法定全称、联系地址、统一社会信用代码；如品牌持有者和运营公司不同，说明关系。注册地址仅在与联系地址不同且需披露时补充。

### P03 客服、举报与筑梦师

**代码已确认**

- 在线客服入口已经存在：底部“筑梦师” → 客服列表 → 会话页面。
- 客服列表通过 `/api/im/customers` 获取，会话、消息和附件走业务后台及腾讯 IM。
- 支持文字、图片、视频；H5 另支持选择音频文件，当前原生端选择音频文件提示不支持。
- 内容举报已有业务接口；接口文档还约定支持内容、评论和用户举报，由后台管理员审核。
- “帮助与反馈”和“视频替换诉求”表单只写入本地记录，不能按页面成功提示认定已送达客服。

**仍需提供**：客服工作时间、投诉/账号处置申诉的受理方式及答复期限、备用联系邮箱或电话；筑梦师实际服务范围、是否收费、人员所属公司/外包关系、资质及后台可查看的信息范围。

依据：`pages/match/match.vue`、`pages/match/chat.vue`、`api/im.js`、`api/report.js`、`pages/profile/sub.vue:780`、`pages/profile/sub.vue:797`；接口文档第 1346 行。

### P04 个人信息保护联系渠道

**核对结果**：未找到专用隐私联系人、部门、邮箱和通信地址。“筑梦师”是已存在的一般客服入口，但没有证据表明已承担注销或隐私申请受理职责。

**仍需提供**：个人信息保护联系人或部门、邮箱、通信地址及备用方式。如接受不满十四周岁用户，补充监护人申请受理安排。

### P05 协议版本与日期

**代码已确认**：应用配置有版本号，原协议页仍为占位内容，未找到正式协议的版本、生效日期及历史记录。

**仍需提供**

| 文件 | 协议版本号 | 更新日期 | 生效日期 |
| --- | --- | --- | --- |
| 用户协议 | 待填写 | 待填写 | 待填写 |
| 隐私政策 | 待填写 | 待填写 | 待填写 |

另需确定首次发布/变更通知及历史版本查询方式。不能直接把本次核对日期当作生效日期。

依据：`pages/settings/about.vue:54`、`androidPrivacy.json`、`manifest.json`。

### P06 年龄与未成年人

**代码已确认**：资料表单要求性别、年龄；年龄校验范围为 **1-120 岁**。未找到最低使用年龄准入、监护人同意或儿童专门规则。

**仍需提供**：最低服务年龄、是否接受未成年人及不满十四周岁用户、相应功能限制和监护人流程。年龄填写范围不等于实际服务年龄规则。

依据：`pages/settings/edit-profile.vue:189`、`pages/profile/sub.vue:862`。

### P07 收费、邀请和 AI 额度

**代码已确认**

- 额度来自业务接口 `/api/quota/video`，客户端展示每日剩余、奖励余量和累计获得/消耗。
- 当前业务代码未发现支付、订单、订阅或自动续费调用。
- 邀请链接携带 `inviter=用户ID`；但登录页未读取该参数，实际调用仅传入手机号和验证码。API 封装虽支持可选 `inviteCode`，页面尚未传入。因此，当前客户端未形成完整的邀请归因流程。
- `utils/aiQuota.js` 的本地“生成 3 次/替换 1 次”辅助逻辑未被当前页面引用，不能当作当前正式额度规则。

**接口文档已有约定**

| 项目 | 已有约定 |
| --- | --- |
| 每日额度重置 | 每日零点自动重置；未写明时区及每日具体分配数量 |
| 注册奖励 | 注册赠送 3 个奖励额度 |
| 邀请奖励 | 每邀请 1 人奖励 1 个额度 |
| 奖励有效期 | 永久有效 |
| 扣减顺序 | 优先使用每日首发额度，再使用奖励额度 |
| 发布时开启视频生成 | 同步扣减额度、进入生成中状态；额度不足则发布失败 |

**仍需提供**：每日具体数量、重置时区、有效邀请认定、失败或审核拒绝是否返还、作弊及额度争议处理。上述文档约定如已调整，只需给出差异。当前未发现收费实现；仅在实际另有收费业务时补充价格、支付、退款、续费规则。

**开发核实事项**：接通邀请参数和注册归因，验证实际后台额度与已有约定一致；不应把缺失的调用流程写成已可获得邀请奖励。

依据：`api/quota.js`、`pages/publish/publish.vue`、`pages/profile/profile.vue:183`、`pages/login/login.vue:152`、`api/auth.js:28`、`utils/aiQuota.js`；接口文档第 455、488、1086、1113-1117 行。

### P08 账号和个人信息权利

**代码已确认的可用操作**

| 操作 | 已有页面或接口 |
| --- | --- |
| 查阅、更正昵称、头像、性别、年龄和简介 | “我” → 编辑资料；`api/user.js` |
| 更换绑定手机号 | “我” → 账号管理；`/api/auth/rebind-phone` |
| 删除自己发布的心情 | “我” → 我的内容 → 已发布 → 删除 |
| 删除本人评论、取消点赞/收藏 | 内容详情及对应互动入口 |
| 管理麦克风等设备授权 | 系统/浏览器权限设置；App 已有麦克风设置引导 |
| 退出登录 | “我” → 退出登录；`/api/auth/logout` |

**接口文档已有约定**：心情和评论删除采用软删除；退出登录使当前 token 失效。软删除不代表文件、数据库记录及备份全部清除。

**仍需提供**：当前尚未实现的账号注销、复制导出、撤回同意和人工申请渠道；最少核验材料、处理时限、通知及申诉方式；后台和服务商副本的删除范围及合法保留例外。已存在的资料修改、换绑、内容删除入口无需重新填写。

依据：`pages/profile/profile.vue`、`pages/profile/sub.vue:696`、`pages/settings/edit-profile.vue`、`pages/space/detail.vue`、`utils/voiceRecord.js`、`api/auth.js`；接口文档第 273、946、1898 行。

### P09 保存期限与清理

**代码或接口文档已能确定的规则**

| 数据 | 已查明的处理 | 还缺什么 |
| --- | --- | --- |
| 验证码 | 接口文档：6 位数字，5 分钟有效，60 秒内只能发送一次；客户端有 60 秒倒计时 | 后台原始验证码存储方式、发送/验证日志期限；有效期不等于日志保留期 |
| 草稿 | 页面调用服务器草稿接口；文档约定每种内容类型只留最新一条、保存时覆盖、发布成功自动清除，并提供删除接口 | 长期未发布草稿的过期规则；草稿引用文件的实际清理 |
| 心情和评论 | 文档约定仅能删除本人内容，采用软删除 | 软删除记录彻底清理/匿名化时间、媒体访问失效和备份处理 |
| 登录与偏好缓存 | 本地保存用户资料、token、登录态、主题、语言和设置；未设应用层固定过期时间 | 后台 token 有效期及打包运行时额外缓存 |
| 退出登录 | 清除/重置用户资料、token、登录态；未同时清除所有其他本地记录 | 注销及跨系统删除机制，见 P08 |
| 发布入口暂存 | 本地 `publish_entry_intent_v1` 保存选中的心情/场景，消费时移除 | 该项已有清理条件，无需另填固定天数 |
| 反馈、视频替换表单 | 本地 `profile_service_records_v1`，未见固定过期或随退出清理 | 如需作为正式受理记录，先落实联网流程与保留规则 |
| 本地录音 | 完成后尝试上传；主动删除/重录会清理对应本地文件；页面卸载未统一清理全部录音文件 | 放弃/未发布文件及上传副本的后台清理期限 |
| 客服消息与附件 | 业务后台和 IM 均参与；关闭会话调用结束会话流程，不等于删除消息 | 两处分别的保存期限和删除机制 |
| 搜索、浏览、匹配 | 客户端发起查询、浏览上报和匹配请求；未发现业务层本地搜索/浏览历史存储实现 | 后台是否保存历史、具体字段及期限 |

**仍需后台/运维填写的保留规则**

| 信息类别 | 保存期限及起算条件 | 到期/申请删除方式及保留例外 |
| --- | --- | --- |
| 账号、手机号、个人资料及登录凭证 | 待填写 | 待填写 |
| 验证码发送/校验、安全、访问、故障日志 | 待填写 | 待填写 |
| 心情、评论、点赞、收藏、消息及软删除记录 | 待填写 | 待填写 |
| 搜索/浏览/匹配日志（如实际保存） | 待填写或不保存 | 待填写 |
| 未发布草稿、孤立上传、替换或放弃的音频 | 待填写 | 待填写 |
| ASR 音频、转写、识别日志和后台回调数据 | 待填写 | 待填写 |
| AI 输入、输出、失败任务、审核及任务日志 | 待填写 | 待填写 |
| 客服会话与附件，业务后台/IM 分别列出 | 待填写 | 待填写 |
| 举报、投诉、权利申请、邀请关系和额度记录 | 待填写 | 待填写 |
| 对象存储、CDN 缓存和备份副本 | 待填写 | 待填写 |

请给出实际期限或可执行的计算规则；备份还需说明轮换及恢复时同步删除的方式。本地已有规则见上表，不再重复要求填写。

依据：`store/index.js:48`、`store/index.js:190`、`utils/publishEntryIntent.js`、`pages/profile/sub.vue:617`、`pages/publish/publish.vue:366`、`pages/publish/publish.vue:942`、`pages/publish/publish.vue:1673`、`utils/moodView.js`；接口文档第 780、1960、2038、2142 行。

### P10 存储地域与跨境

**代码已确认**：客户端业务基地址使用 HTTPS；发现腾讯 IM、Android 腾讯 ASR、浏览器语音识别及后台上传/媒体接口。接口文档还列出腾讯云 VOD、ASR 回调。

**仍需提供**：业务数据库、文件/备份、CDN、IM、ASR、AI、短信、审核各自的实际存储和处理国家/地区；是否存在境外服务、远程运维或子处理者访问。有跨境处理时补充实际接收方、联系方式、权利申请方式及适用程序。

接口文档中的 MinIO、OSS 示例地址不是实际部署证明；域名和 HTTPS 也不能证明全部境内处理。

依据：`utils/request.js:17`、`utils/tencentImClient.js`、`utils/tencentSpeechAsr.js`；接口文档第 1413、1458、2049 行。

### P11 短信服务

**代码已确认**：手机号和验证码通过业务认证接口发送/校验，客户端未直接集成短信发送 SDK。发送倒计时已有 60 秒实现。

**接口文档已有约定**：6 位验证码、5 分钟有效；同一段同时写有“短信服务预留，当前仅记录日志”。

**仍需提供**：实际后台是否已接通短信；如已接通，提供当前通道供应商、法定主体和政策链接、额外接收字段、日志保留及删除规则。无需重新填写验证码长度和文档已有时效，若实际调整则说明差异。

依据：`api/auth.js`、`pages/login/login.vue:113`；接口文档第 780 行。

### P12 腾讯云即时通信 IM

**代码已确认**

- SDK 为 `tim-js-sdk`，依赖锁文件版本 **2.27.6**。
- 用途为筑梦师客服会话的鉴权、实时收发及附件信息传递。
- 客户端处理 IM 用户标识、SDK 应用标识、登录签名、发送/接收方、消息内容、附件地址/元信息、消息 ID 和时间。
- 后台另有消息列表与发送接口；客服会话存在业务后台和 IM 两条处理链路。

**仍需提供**：实际接收方/签约主体、处理关系及有效隐私政策链接；SDK 独立采集的设备、网络、日志字段；实际区域、消息留存及删除配置。SDK 名称、锁定版本、已知业务字段和触发场景已填，不再重复索要。

依据：`package-lock.json:11492`、`utils/tencentImClient.js`、`api/im.js`、`pages/match/chat.vue`。

### P13 腾讯云语音识别 ASR

**代码已确认**

- **Android 启用原生实时识别，iOS 当前未启用腾讯原生实时识别。**
- Android 活跃插件使用的 `asr-realtime-release.aar` 与资料包 `QCloudAsrSDK_Android_v3.1.39` 中对应文件 SHA-256 完全一致，可确认当前源码依赖文件对应 **3.1.39**。
- 两个文件的 SHA-256：`E8BEC3C044E49501A1EEC383C944D519A3538016E6AE247E91E5934DE31B05AD`。
- 本地插件自身版本是 **1.0.0**，这是插件版本，不是腾讯 SDK 版本。
- iOS 虽有 3.1.37 资料包，但插件返回不支持、frameworks 为空，不能列为已启用的 iOS SDK。当前 iOS 走普通录音流程；后续后台是否识别另行核实。
- Android 主动录音时调用 SDK，默认模型参数为 `16k_zh`，处理音频流、转写结果和鉴权信息。
- 代码支持后台临时凭证接口及开发凭证分支，发布页显式优先开发凭证；不能写成“所有情况均通过后台 STS”。凭证值无需提供到协议资料中。
- 接口文档另有后台 ASR 回调，不能与客户端 Android 实时识别混为同一处理路径。

**仍需提供**：实际接收方/合同主体和政策链接、SDK 额外独立采集字段、音频/日志的地域与保留规则、训练或人工查看配置；后台 ASR 回调是否实际启用及用途。正式成包版本只需由打包验证确认是否与上述源码依赖一致，无需再次猜测两个平台的版本。

依据：`utils/tencentSpeechAsr.js:25`、`pages/publish/publish.vue:1332`、`uni_modules/tencent-speech-asr/package.json`、`uni_modules/tencent-speech-asr/utssdk/app-android/TencentSpeechAsrNative.kt:326`、`uni_modules/tencent-speech-asr/utssdk/app-ios/index.uts:8`、`uni_modules/tencent-speech-asr/utssdk/app-ios/config.json`；接口文档第 1458 行。

### P14 DCloud / uni-app

**代码已确认**：项目使用 uni-app；`@dcloudio` 相关依赖声明版本为 `3.0.0-4020920240930001`。配置包含 Camera、Gallery、Record、VideoPlayer、Webview-x5 模块，`uniStatistics.enable=false`。客户端启动初始化有系统信息和网络状态读取。

**仍需提供**：实际打包使用的 HBuilderX/原生运行时版本及厂商有效政策链接；运行时在业务调用之外独立采集和上传的字段、触发时点及留存。依赖版本不能直接替代最终原生运行时版本，关闭统计也不能证明无其他数据处理。

依据：`package.json`、`manifest.json:18`、`main.js:32`、`store/index.js`。

### P15 腾讯 X5

**代码已确认**：App 模块声明包含 `Webview-x5`。

**仍需打包人员核实**：最终安装包是否实际包含并启用 X5；若包含，补实际版本、主体/政策链接、初始化场景及额外数据处理。若未实际启用，给出成包依据后从正式第三方清单移除。

依据：`manifest.json:23`。模块声明已经查明，无需再次让运营人员填写模块名称。

### P16 AI 视频生成

**代码已确认**：客户端支持发布时请求生成、已有心情转视频、生成状态查询和服务端额度展示；提交心情文字、语音地址/时长、转写及相关任务信息。

**特别核对**：选择“转为文字发布”后，当前构造的发布请求仍可能携带已有 `voiceUrl`、`voiceDuration`、`voiceText`，不是只发送文字。录音在发布前已经上传，后台实际交给模型的字段尚无源码依据。

**接口文档已有约定**：生成结果写入展示内容时重新进入待审核；文档部分段落仍把文本转视频服务标为“预留”，因此不能据文档认定当前线上模型。

**仍需提供**：实际 AI 服务商/模型、提交到模型的字段、训练和人工查看用途、输入输出及失败任务保留/删除、生成内容标识和输出使用限制。已确认的客户端请求与额度机制无需重填。

依据：`api/mood.js`、`pages/publish/publish.vue:1725`、`utils/apiAdapter.js`、`pages/profile/sub.vue:691`；接口文档第 455、585 行。

### P17 云存储、VOD、CDN 与审核

**代码已确认**：存在头像、录音、客服附件上传和媒体访问流程，以及审核状态字段。

**接口文档已有信息**

- 已列出腾讯云 VOD 事件通知接口 `/api/pipeline/vod/callback` 和腾讯云 ASR 回调 `/api/asr/callback`。这是后台流程线索，尚非实际部署证明。
- 当前头像使用 `/api/upload/avatar`；对应文档为 jpg/png/gif/webp、最大 5MB，随后更新用户资料。
- 语音上传文档为 mp3/m4a/aac/wav/ogg、最大 20MB；通用上传最大 20MB。
- 旧 `/api/user/avatar` 的“OSS 预留/建议 2MB/返回空地址”描述不适用于当前头像调用。文档中的 OSS、MinIO 示例也不能用来填写实际供应商。

**仍需提供**：服务器/数据库、对象存储、CDN 和审核的实际供应商或自建部署信息；VOD 与后台 ASR 是否实际启用、处理的媒体/结果及保留规则；各实际接收方主体、政策链接、地域和人员访问范围。

依据：`api/upload.js`、`api/user.js`、`pages/settings/edit-profile.vue`、`pages/match/chat.vue`；接口文档第 1157、1219、1281、1413、1458、2369 行。

### P18 权限、SDK 与本地存储

**代码已确认的权限/能力**

| 类别 | 当前配置或调用 | 核对结论 |
| --- | --- | --- |
| 麦克风 | Android RECORD_AUDIO；iOS NSMicrophoneUsageDescription；录音权限申请 | 对应主动录音与 Android 实时转写 |
| 相机 | CAMERA；NSCameraUsageDescription；选择图片/媒体流程 | 对应用户主动拍摄、头像及客服媒体选择 |
| 相册/存储 | READ_EXTERNAL_STORAGE、WRITE_EXTERNAL_STORAGE、READ_MEDIA_IMAGES；NSPhotoLibraryUsageDescription | 对应文件、图片/媒体选择；新 Android 版本实际授权行为需成包验证 |
| iOS 写入相册 | NSPhotoLibraryAddUsageDescription | 已声明；未检索到明确的业务保存到相册调用，需核实必要性 |
| 网络状态 | ACCESS_NETWORK_STATE、ACCESS_WIFI_STATE；网络类型读取 | 有运行/网络判断用途；声明本身不证明上传所有网络字段 |
| 其他系统能力 | VIBRATE、WAKE_LOCK、FLASHLIGHT、MODIFY_AUDIO_SETTINGS | 已声明；需结合运行框架/原生 SDK 核实具体使用 |
| 用途尚不明确 | READ_PHONE_STATE、GET_ACCOUNTS、READ_LOGS、WRITE_SETTINGS、MOUNT_UNMOUNT_FILESYSTEMS、CHANGE_NETWORK_STATE、CHANGE_WIFI_STATE | 当前业务代码未找到明确必要用途，由开发核实是否移除 |
| 剪贴板 | 主动复制邀请链接、部分客服外链时写入 | 未发现读取剪贴板历史的业务调用 |
| 定位、通讯录、短信收件箱 | 未发现对应业务调用 | 不列为已确认业务收集项目 |

相机硬件及自动对焦的 `uses-feature` 是能力声明，不应计为额外权限。本地存储名称、用途和清理条件已写入 P09；已知 SDK 及平台差异见 P12-P15、P21。当前业务代码未发现另接广告、支付、推送的调用。

**仍需开发提供**：最终安装包合并权限及间接 SDK 的差异清单；声明但用途不明项的核实结果；SDK/网页部署额外 Cookie 或标识的处理情况。无需重新整理当前源码中已列出的权限和本地缓存。

依据：`manifest.json:59`、`utils/voiceRecord.js`、`utils/chooseImage.js`、`pages/profile/profile.vue:205`、`pages/match/chat.vue:598`、`store/index.js`。

### P19 安全与同意机制

**代码已确认**

- 业务接口配置使用 HTTPS，通过登录 token 访问需要鉴权的接口。
- 登录页协议默认未勾选；登录动作有勾选校验，但发送验证码动作未校验该勾选。
- `main.js` 启动时调用状态初始化，涉及系统和网络信息读取；没有据此证明原生隐私弹窗之前就已采集，成包时序还需实测。
- 系统隐私提示有配置文件，但正式主体、政策链接及业务页面仍需一致接入。
- 未找到完整的政策版本、同意和撤回事件持久化机制。
- ASR 支持开发凭证优先分支，见 P13；正式打包应由开发核查凭证来源与日志。

**仍需提供**：服务端静态加密/密钥管理、文件鉴权、人员访问、日志脱敏、备份删除和事件响应的实际措施；首次告知、拒绝、撤回、单独同意及版本记录的落地结果。HTTPS、默认勾选状态及调用位置已经查明，无需再填。

依据：`utils/request.js:17`、`pages/login/login.vue:81`、`pages/login/login.vue:113`、`pages/login/login.vue:141`、`main.js:32`、`store/index.js`、`androidPrivacy.json`。

### P20 资料可见范围、匹配与审核

**代码已确认**

- 保存资料会提交昵称、头像、简介、性别和年龄；当前表单要求昵称、性别、年龄。
- 内容作者区域及进入他人内容页的导航包含用户 ID、昵称、头像；未发现足以确认向他人展示年龄、性别、简介的页面依据。
- 他人内容页隐藏自己的收藏管理及编辑/删除按钮。这是前端控制，后台仍需验证越权防护。
- 广场有时间、今日热度、全部热度等排序和关键词/标签筛选；客户端匹配请求传入所选 `moodId`，兼容多条匹配结果。
- 当前 App 有全局登录检查，不能把文档的匿名公共接口能力写成 App 已开放游客模式。
- 邀请归因未接通，详见 P07；未发现已实现的业务层本地搜索/浏览历史，详见 P09。

**接口文档已有约定及冲突**

| 项目 | 文档已有规则 | 当前结论 |
| --- | --- | --- |
| 内容访问 | 公开内容任何人可见；待审核、拒绝、违规下线内容仅作者可见 | 已有访问规则，不再作为全空待填项；后台访问控制须验证 |
| 心情公开时点 | 发布接口又写“发布后直接出现在广场” | 与审核状态的关系不明确，需后台解释正常发布的状态转换 |
| 评论 | 先进入待审核，通过后展示 | 已写入待确认稿，保留部署核实 |
| 生成视频 | 生成结果替换展示时重新待审核 | 已有约定，需核实实际流程 |
| 草稿 | 当前用户、按类型最新一条，发布成功清除 | 已有归属和生命周期规则，附件访问及过期另见 P09 |
| 匹配 | 随机一条，与“用户最近发布内容”的类型相近，排除本人 | 与客户端“选择 moodId、支持多条结果”不一致，不能当作已验证算法 |

**仍需提供**：性别/年龄必填的实际必要性；后台对他人返回的完整资料字段；上述公开时点和匹配冲突的实际规则；是否另外使用画像/推断情绪，若涉及个性化推荐，其关闭与用户选择方式；内容删除后媒体和副本处理。已有显示字段、排序选项及文档访问约定已填入。

依据：`utils/userProfileNav.js`、`pages/profile/sub.vue:421`、`pages/index/index.vue:392`、`api/mood.js:96`、`App.vue`；接口文档第 44、332、455、585、668、1793、2038 行。

### P21 H5 浏览器语音识别

**代码已确认**：网页端在浏览器支持时调用 `SpeechRecognition` / `webkitSpeechRecognition`，与 Android 腾讯原生 ASR 为不同实现；当前代码不能指定所有浏览器使用同一识别供应商，也不能证明完全本地识别。

**仍需提供**：H5 是否实际发布并保留此能力；实际支持的浏览器及识别服务提供者、隐私政策链接、数据地域/留存；识别前告知与同意安排。若不发布 H5，正式政策可按实际范围移除该分支。

依据：`pages/publish/publish.vue:1425`。

## 三、第三方资料只需补差额

已知的 SDK 名称、源码版本、用途、客户端业务字段和触发场景已写在 P11-P17、P21。对每家实际接收个人信息的服务商，仅补以下仍缺内容；相同服务商可合并说明，不必重复填写整套空表。

- 法定接收方/提供者、联系方式、合同主体及委托或独立处理关系。
- 有效的官方隐私政策链接。
- 业务源码之外独立采集的字段、方式、频次及初始化时点。
- 实际处理/存储地域、保留期限、用户申请删除及服务终止后的处理。
- 与音频或 AI 有关的训练、服务改进、人工查看配置及退出能力。
- 实际正式版本与本文件已核对源码的差异。

当前源码无法代替服务商控制台、合同和正式成包信息。无需提供密钥、用户真实数据或验证码。

## 四、已发现的实现差异

以下是应由开发或运营落实的事项，不是让你补写不存在的事实。本次只修订协议资料，没有修改 App 功能。

- [ ] 统一橘子圆梦/心情星球名称及版本显示，将正式协议接入登录页、关于页和系统隐私提示，验证未登录可阅读。
- [ ] 完成首次发送手机号前的告知及必要确认，核验初始化和 SDK 在同意前/拒绝后的行为，并补齐版本、同意和撤回记录。
- [ ] 接通邀请链接参数与注册归因，验证零点重置、注册赠 3、邀请加 1、扣减顺序等文档规则与实际后台一致。
- [ ] 核实短信“预留仅日志”、AI“预留”说明是否已过时；以当前后台结果修正接口文档。
- [ ] 补齐注销、导出、撤回和人工删除申请的真实受理流程，不把退出登录当作注销。
- [ ] 录音前明确 Android 实时转写与发布前自动上传；核实“转为文字发布”仍携带音频字段的必要性，落实未发布/放弃音频的清理。
- [ ] 修正“资料、草稿、收藏、客服仅保存在本地”等与当前接口不符的旧说明。
- [ ] 接通反馈和视频替换诉求的后台受理，或修正只保存本地却提示已提交的状态。
- [ ] 核实用途不明的权限、X5、正式 SDK 及 ASR 凭证分支；iOS 未启用的实时 ASR 不得列为已接入。
- [ ] 落实年龄准入和资料字段必要性，以及适用的监护人流程。
- [ ] 对齐匹配输入/数量、发布与审核状态规则，验证草稿、非公开内容、他人资料、客服附件和被删媒体的访问控制。
- [ ] 补实 AI 接收方、模型数据用途、内容标识和输出限制；按实际上线范围处理 H5 语音服务。

## 五、核对范围与资料回填

本次证据来自当前文件夹中的客户端、配置、SDK 文件及接口文档，不包含后台实现、云控制台、真实网络抓包或上架安装包。代码、接口文档冲突时已列出双方依据，没有把旧文档规则当作线上已验证事实。

两份 HTML 已同步补入可确认的信息并收窄占位。后续收到剩余资料后，按 P 编号回填；上线前去掉内部核对说明，确定版本/日期，并使协议内容与实际功能和后台处理保持一致。
